.DS_Store
__pycache__/
*.pyc
.venv/
# voice assistant: wake-word model is large binary data, download separately
assistant/wakeword-model/
# the ring's live runtime files (see state/README.md)
state/state
state/mood.json
state/wave.json
# your personal config, created at setup: never tracked, never touched by updates
barehands.json
# secrets for the voice assistant (GEMINI_API_KEY etc.) -- never tracked
.env
