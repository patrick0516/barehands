# holo/

The same model files render as the blue hologram wireframe here. The folder IS the render law.

`DamagedHelmet.glb` here is a symlink to `../models/DamagedHelmet.glb`
(CC-BY-4.0, theblueturtle_, via Khronos Group's glTF-Sample-Assets) —
see `../models/README.md` for full credits.
